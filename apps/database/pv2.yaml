apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv-2
spec:
  capacity:
    storage: 1Gi
  accessModes:
  - ReadWriteOnce
  storageClassName: local-path
  hostPath:
    path: /data/postgres-2
  claimRef:
    namespace: dev
    name: postgres-data-postgres-2

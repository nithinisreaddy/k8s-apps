apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv-0
spec:
  capacity:
    storage: 1Gi
  accessModes:
  - ReadWriteOnce
  storageClassName: local-path
  hostPath:
    path: /data/postgres-0
  claimRef:
    namespace: dev
    name: postgres-data-postgres-0
